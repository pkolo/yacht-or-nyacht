<p>(Select the result with the most comprehensive track credits)</p>
<% @results.each do |result| %>
<div class="search-result">
  <a href="https://www.discogs.com<%= result['uri'] %>" target="_blank"><%= result["title"] %></a> (<%= result["year"] %> / <%= result["country"] %> / <%= result["format"].join(", ") %>)
  <form class="add-personnel-btn" action="add_personnel" method="POST">
    <input type="hidden" name="url" value="https://api.discogs.com/releases/<%= result['id'] %>">
    <input type="submit">
  </form>
</div>
<% end %>
