<% songs.each do |song| %>
  <div class="row">
    <div class="yachtski avg col-2">
      <%= song.yachtski.round(2) %>
    </div>
    <div class="col-10">
      <%= song.title %>
    </div>
  </div>
<% end %>
