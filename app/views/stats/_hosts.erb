<div class="stats-container">

  <% @hosts.each_with_index do |host, index| %>
    <% if index.even? %>
      <div class="row">
    <% end %>

      <%= erb :'hosts/_stats', locals: {host: host} %>

    <% if index.odd? %>
      </div>
    <% end %>

  <% end %>

</div>
